# 0622 ToDo

Implementation agent: read `docs/todos/0622-plan.md` first. After each item is completed and tested, change `[ ]` to `[x]`.

- [ ] 01. Fix PathSandbox: resolve `..` in non-existing paths, add workspace existence check.
  - Instruction: `docs/todos/0622/01-fix-path-sandbox.md`

Final verification:

- [ ] `docker compose run --rm test` — All tests pass
- [ ] `docker compose run --rm test sh -lc "git config --global --add safe.directory /app && composer validate --no-check-publish"`
- [ ] `docker compose run --rm test sh -lc "find . -path ./vendor -prune -o -name '*.php' -print -exec php -l {} \;"`
- [ ] `docker compose run --rm test sh -lc "composer build && php php-agent.phar --help"`
